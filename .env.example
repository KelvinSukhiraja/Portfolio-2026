# Sanity — copy to .env.local and fill in values from sanity.io/manage → API → Tokens
VITE_SANITY_PROJECT_ID=mp9gw1i2
VITE_SANITY_DATASET=production
# Optional: read token for draft/private datasets (create at sanity.io/manage)
VITE_SANITY_API_READ_TOKEN=
